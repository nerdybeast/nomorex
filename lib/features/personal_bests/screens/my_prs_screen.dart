import 'package:flutter/material.dart';

class MyPrsScreen extends StatelessWidget {
  const MyPrsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('My PRs — coming soon')),
    );
  }
}
